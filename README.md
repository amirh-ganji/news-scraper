# 📰 Tasnim News Scraper

یک اسکریپت Python برای استخراج خودکار عناوین و لینک‌های اخبار اقتصادی از سایت خبرگزاری تسنیم.

---

## 📋 توضیح پروژه

این پروژه با استفاده از `requests` و `BeautifulSoup` صفحه اخبار اقتصاد ایران سایت تسنیم را پردازش می‌کند و عنوان و لینک هر خبر را در یک فایل CSV ذخیره می‌نماید.

---

## ⚙️ پیش‌نیازها

قبل از اجرا، کتابخانه‌های مورد نیاز را نصب کنید:

```bash
pip install requests beautifulsoup4
```

---

## 🚀 نحوه اجرا

```bash
python main.py
```

پس از اجرا، فایل `data.csv` در همان مسیر ایجاد می‌شود.

---

## 📁 خروجی

فایل `data.csv` شامل دو ستون است:

| عنوان خبر | لینک خبر |
|-----------|----------|
| عنوان مقاله | https://www.tasnimnews.com/... |

---

## 🔗 منبع داده

- **سایت:** [خبرگزاری تسنیم](https://www.tasnimnews.com)
- **بخش:** اخبار اقتصاد ایران

---

## 📂 ساختار پروژه

```
tasnim-news-scraper/
│
├── main.py       # اسکریپت اصلی
├── data.csv      # خروجی (پس از اجرا ایجاد می‌شود)
└── README.md     # این فایل
```

---

## 📝 نکات

- اسکریپت هر بار اجرا شود، فایل CSV را بازنویسی می‌کند.
- در صورت تغییر ساختار HTML سایت تسنیم، ممکن است نیاز به بروزرسانی CSS selector‌ها باشد.
